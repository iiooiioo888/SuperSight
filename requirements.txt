# SuperSight V3.0 依賴清單
# 安裝方式: pip install -r requirements.txt
# 注意: 部分組件需要額外系統依賴（見 README.md）
#
# V3.0 升級：
# - LangGraph 0.5.x: 原生異步並行分支
# - ChromaDB 1.2.x: 混合檢索 + HNSW 優化
# - bge-m4: 1024維多模態嵌入
# - Qwen3-VL-8B: FP4 量化 (RTX 50 系列原生加速)
# - PyTorch 2.6+: CUDA 12.8 (Blackwell 支援)

# ─── 核心框架 ────────────────────────────────────
langgraph>=0.5.0
langchain>=0.3.0
langchain-community>=0.3.0

# ─── Web 界面 ────────────────────────────────────
gradio>=5.0.0

# ─── 視覺模型 (Ollama API) ───────────────────────
requests>=2.31.0

# ─── 人臉分析 ────────────────────────────────────
insightface>=0.7.3
onnxruntime-gpu>=1.17.0
opencv-contrib-python>=4.9.0.80

# ─── 情緒識別 ────────────────────────────────────
deepface>=0.0.79

# ─── 向量數據庫 (V3.0: ChromaDB 1.2.x) ────────────
chromadb>=1.2.0

# ─── Embedding 模型 (V3.0: bge-m4 1024維) ────────
# bge-m4 需要 sentence-transformers>=3.0.0
sentence-transformers>=3.0.0

# ─── 圖片處理 ────────────────────────────────────
Pillow>=10.3.0

# ─── 系統工具 ────────────────────────────────────
numpy>=1.26.0

# ─── GPU 加速 (V3.0: CUDA 12.8 for Blackwell) ──────
# CUDA 12.8: pip install torch==2.6.* --index-url https://download.pytorch.org/whl/cu128
# CUDA 12.1: pip install torch==2.6.* --index-url https://download.pytorch.org/whl/cu121
# CPU:       pip install torch==2.6.* --index-url https://download.pytorch.org/whl/cpu
torch>=2.6.0
torchvision>=0.19.0